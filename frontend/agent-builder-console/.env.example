VITE_AGENT_BUILDER_API_BASE_URL=http://localhost:8002/api/v1
# iam-service (for silently refreshing the access token directly, without a round trip through
# the portal) and portal (the platform's sole login page/launcher - an unauthenticated visitor is
# redirected there to log in, and it redirects back here on success).
VITE_IAM_SERVICE_URL=http://localhost:8003
VITE_PORTAL_URL=http://localhost:5175
